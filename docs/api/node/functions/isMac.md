[@longmo-utils/node](../index.md) / isMac

# Function: isMac()

```ts
function isMac(): boolean;
```

检查当前平台是否为 macOS

## Returns

`boolean`

是否为 macOS

## Example

```ts
if (isMac()) {
  console.log('Running on macOS')
}
```
