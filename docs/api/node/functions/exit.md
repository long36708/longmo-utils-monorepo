[@longmo-utils/node](../index.md) / exit

# Function: exit()

```ts
function exit(code): never;
```

Exits the process

## Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `code` | `number` | `0` |

## Returns

`never`
