[@longmo-utils/common](../index.md) / isValidColor

# Function: isValidColor()

```ts
function isValidColor(color?): boolean;
```

检查颜色是否有效

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `color?` | `string` | 待检查的颜色 如果颜色有效返回true，否则返回false |

## Returns

`boolean`
